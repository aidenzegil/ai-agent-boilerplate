export type PublicUser = {
  username: string;
  email: string;
  id: string;
  profilePictureUrl: string;
};

export type PrivateUser = {
  username: string;
  email: string;
  id: string;
  profilePictureUrl: string;
};
